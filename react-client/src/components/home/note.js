const Note = () => {
    return (
        <div className="d-flex align-items-center" style={{ height: "80vh" }}>
            <div className="container px-4 px-lg-5 text-center">
                <h3 className="mb-1">Note :</h3>
                <h5 className="mb-5"><em>if you register with "admin" name, then you will get different features</em></h5>
            </div>
        </div>
    );
};

export default Note;